Flask==3.0.0
flask-cors==4.0.0
textblob==0.17.1
openai==1.97.1
python-dotenv==1.0.0
gunicorn==21.2.0
# Lightweight emotion analysis - only 5MB RAM usage
vaderSentiment==3.3.2
# Note: Heavy models (torch, transformers) are intentionally excluded
# This prevents Render from trying to install 500MB+ dependencies
